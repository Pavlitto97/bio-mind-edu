name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: [ github-actions-deploy ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Prepare build environment
        run: |
          cd biomind_edu
          mkdir -p assets/animations
          echo "# Environment file for CI/CD" > .env
      
      - name: Get dependencies
        run: |
          cd biomind_edu
          flutter pub get
      
      - name: Generate localizations
        run: |
          cd biomind_edu
          flutter gen-l10n || echo "Localizations generation skipped"
      
      - name: Build web
        run: |
          cd biomind_edu
          flutter build web --release --base-href /bio-mind-edu/ --no-tree-shake-icons --target lib/main_simple.dart
      
      - name: Add .nojekyll file
        run: touch biomind_edu/build/web/.nojekyll
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'biomind_edu/build/web'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
